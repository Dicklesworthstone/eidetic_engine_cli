name: Verify operational memory recovery
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    uses: ./.github/workflows/recorded-provenance-20260919.yml
    permissions:
      contents: read
