name: Ask delivery (retired)

# Native procedural-rule answering is committed on main at
# af22f1611eaa5e11b3a8af9676235ac8d042ccf6 after its production library check.
# Run 35303424374 records engine and existing CLI tests independently.
# No automatic trigger, source writer, or ref update remains here.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-time source delivery is retired.'
