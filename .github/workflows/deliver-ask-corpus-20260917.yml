name: Ask delivery (retired)

# Completed one-time delivery directly to main, without PRs or force pushes.
# Actions run 35254991516:
# d40e84b4daf5757a45411e3dc4fd822c6565ca40 - complete scoped counterevidence
# Actions run 35255837784:
# 0ceafad1dbd578107c024606c6f52c996a2d05c1 - exact sparse span clustering
# Nightly rustfmt parsed the integration and formatted ask-specific modules.
# Added tests have not been executed; compilation/Clippy are not certified.
# No automatic trigger, write permission, or delivery commands remain.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-time delivery is retired.'
