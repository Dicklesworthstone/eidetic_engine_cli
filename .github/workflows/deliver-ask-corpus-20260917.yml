name: Ask delivery (retired)

# The read-only CLI implementation is on main at
# a0a93ec56d537ba6288f9392463a63aa33c92eba.
# Run 35293588723 records its exact-source verification independently.
# URI privacy repair b7c5b003b5784ea76915720b202e1be9198ce15f passed
# the production library check, 124 engine tests and 5 real CLI tests.
# No source writer or automatic delivery trigger remains in this workflow.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-time delivery is retired.'
