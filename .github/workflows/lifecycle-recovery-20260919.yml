name: Curation and procedure recovery verification
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    uses: ./.github/workflows/primary-recovery-20260919.yml
    permissions:
      contents: read
