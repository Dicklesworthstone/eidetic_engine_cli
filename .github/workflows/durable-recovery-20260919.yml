# The implementation landed directly on main in f87f0b671f1fd7117d0c3e84d8ca317db444795d.
# Its production check and all 14 durable recovery regressions passed in run 35415868668.
# Retain the delivery record without granting future automatic writes.
name: Durable recovery publication fence (completed delivery)
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Delivery complete; use the normal source verification paths.'
