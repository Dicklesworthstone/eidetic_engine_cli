name: Native-evidence delivery (retired)

# One-time delivery completed in Actions run 35244730125.
# Production commits, both fast-forwarded directly to main:
# b42f85372722fb91397173ece6fff2e396329673 - native CASS evidence deltas
# daf3bb49e9ad6282fffc299a2dc9c468f1c6ec1c - retained evidence token accounting
# Nightly rustfmt parsing and format checks passed. Rust tests were not run.
# Retained as an inert receipt rather than deleting the file. No push trigger,
# write permission, token-bearing environment, or delivery code remains.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-time delivery is retired.'
