name: Index publication delivery (retired)

# Production changes are on main:
# 0c3cbbe4d84c54617166348a45bfdea13e94c342 - current-owner publication fence
# b1d8b90a65ff516b80a97f180296ed21acb4e884 - generation durability barriers
# 65d6a7726e0e27db763726951f24cec1dcdeae12 - native atomic directory exchange
# Compilation and publication-boundary tests passed before each source commit.
# Run 35307418086 records the final broader index regression result separately.
# This retired delivery has no automatic trigger, source writer or ref update.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-time index publication delivery is retired.'
