name: Read-only recall delivery (retired)

# Production implementation: 4e45c1b0e14ab06e06aeda2ebd9c261a6dd6d8f9.
# Verification run 35380721262 passed compilation, 39 core tests and
# 15 real CLI/golden tests. This one-time delivery is no longer executable.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Read-only recall is integrated; this delivery is retired.'
