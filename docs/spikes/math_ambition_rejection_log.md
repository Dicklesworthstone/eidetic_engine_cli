# Math Ambition Rejection Log

This log records fail-fast decisions for math-ambition beads whose quantitative
acceptance thresholds are not met. It is intentionally append-only: do not
rewrite past decisions after a follow-up bead exists.

Required fields per entry:

- `bead_id`
- `measured_value`
- `expected_value`
- `decision`
- `follow_up_bead`

## Entries

No rejection events recorded yet.
