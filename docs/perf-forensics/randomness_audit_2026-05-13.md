# Randomness source audit — 2026-05-13

Owner: bd-17c65.14.4.1 (N4.1). Auto-generated by
`scripts/audit_randomness_sources.sh`.

- Source commit: `4612ec001675db95be52f8973a94f9dcbae1d93b`
- Generated at: `2026-05-13T05:11:59Z`
- Total findings: **316**

## Summary

### By randomness kind

- `systemtime`: 261
- `filesystem_order`: 20
- `env`: 19
- `hashmap_iter`: 11
- `rng`: 5

### By severity

- `confirmed_drift`: 183
- `latent_risk`: 128
- `deterministic_today`: 5

### By proposed remediation

- `inject_clock`: 261
- `manual_sort`: 20
- `env_var`: 19
- `sort_iter`: 11
- `capability_token`: 5

## Top 10 file paths by hit count

- `src/db/mod.rs`: 50 findings
- `src/core/certificate.rs`: 21 findings
- `src/cli/mod.rs`: 18 findings
- `src/steward/mod.rs`: 15 findings
- `src/core/index.rs`: 13 findings
- `src/core/search.rs`: 10 findings
- `src/core/rehearse.rs`: 9 findings
- `src/core/context.rs`: 8 findings
- `src/core/procedure.rs`: 8 findings
- `src/core/status.rs`: 8 findings

## Notes

- This audit is a static-grep first pass. Each row's
  `fn_path` is the source file (not the enclosing Rust
  fn) because grep does not give us syntactic context;
  N4.3 (the threading refactor) can use ast-grep to lift
  rows to true qualified paths if needed.
- `hashmap_iter` rows enumerate every `HashMap<`
  declaration; some iterations are sorted downstream and
  do not leak determinism. Manual review per row decides
  the actual remediation.
- `ring::rand::SystemRandom` is classified
  `deterministic_today` because its crypto-key surface
  is intentionally non-deterministic and bounded.
- `rows_content_hash` in the inventory pins the
  sorted-rows hash. N4.2's bead body cites this hash to
  prove the design references this specific audit revision.

## Cited from

- bd-17c65.14.4.1 — N4.1 owner bead.
- bd-17c65.14.4.2 — N4.2 (Deterministic<Seed> token);
  cites this audit by `rows_content_hash` before
  opening implementation.
- bd-17c65.10.7 — J7 determinism harness; cross-references
  this audit when extending its `_strip_fields` catalog.
