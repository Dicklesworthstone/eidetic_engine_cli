# bd-smxdr follow-on — e2e scripts that exist but are invoked by nothing.
#
# Recorded 2026-09-16. Each line is an accepted, pre-existing orphan: a suite
# that was committed and wired into no runner. They are baselined rather than
# fixed here because wiring 19 never-executed suites at once would wedge the
# gate on unknown state, which is a decision for the orchestrator, not a
# side effect of adding an audit.
#
# THIS LIST MAY ONLY SHRINK. scripts/e2e_invocation_audit.sh fails in both
# directions: a NEW orphan is an error, and an entry here that has since been
# wired is ALSO an error telling you to delete the line. That is what stops a
# baseline decaying into a permanent ignore-list.
#
# To retire an entry: wire the script into scripts/verify.sh or a tests/*.rs
# harness, then remove its line. To retire the script itself, delete the line
# and the script — but deletion needs the operator's explicit permission
# (AGENTS.md RULE 1).
#
#
# 2026-09-16 bd-udjrq triage: 10 of the 12 non-mesh orphans were wired into
# verify.sh gate 6.12698a-j and removed from this list. The two that remain:
#   e2e_backup_roundtrip.sh    RETIRED - superseded by the registered
#                              tests/e2e_backup_restore_roundtrip.rs target.
#                              File retained per RULE 1; awaiting an operator
#                              decision on deletion.
#   e2e_field_report_suite.sh  BLOCKED - proven to emit false assert_fails
#                              through the unguarded harness PATH fallback
#                              (scripts/lib/e2e_harness.sh:69). Unblocks when
#                              that fallback refuses instead of defaulting.
#
# 2026-09-16 mesh decision: the 7 e2e_mesh_* entries were RETIRED, not wired,
# and moved to tests/fixtures/e2e_invocation/retired.txt. They contain no
# assertions -- each is an RCH dispatch wrapper around a cargo test target CI
# already runs -- so retiring them loses zero assertion coverage. That ledger
# requires a named covering command which the audit verifies actually exists in
# ci.yml or verify.sh, so "retired" cannot be used to park an inconvenient
# orphan. This list still only shrinks.
e2e_backup_roundtrip.sh
e2e_field_report_suite.sh

# 2026-09-16 bd-q2nq9: TWELVE ENTRIES ADDED. Read this before objecting that
# the list grew.
#
# These twelve were ALWAYS orphaned. They are not new debt; they are newly
# VISIBLE debt. The audit's orphan predicate scanned docs/ along with
# everything else, and a Markdown line almost never starts with '#', so an
# English sentence or a table cell naming a script satisfied it:
#     "`scripts/e2e_read_coalescing.sh` asserts this"   docs/read_coalescing.md:73
#     "| e2e | `scripts/e2e_sandbox.sh` | 21.5 |"       docs/agent-ux/.../sandbox.md:31
# A mention was standing in for an invocation -- the same mistake the predicate
# already guarded against for shell COMMENTS, in a second spelling.
#
# Each of the twelve was verified three independent ways to have NO reference in
# scripts/, tests/ or .github/: by hand on the bead, by a per-directory scan,
# and by the corrected audit itself. All three agree.
#
# "THIS LIST MAY ONLY SHRINK" is not being weakened. The rule governs orphans
# the audit can SEE; these were invisible to it. Recording them is the
# correction, and the alternative -- leaving the gate red -- wedges verify.sh
# for every agent rather than making anyone wire a suite. From here the twelve
# are ordinary debt and the list shrinks as before.
#
# True orphan count is 21 of 92 (14 baselined + 7 retired), not 9. The earlier
# "19 of 92" figure in this file's header was measured with the same blind
# predicate and should not be treated as a clean prior.
e2e_diag_contention.sh
e2e_group_commit.sh
e2e_mesh_anti_entropy_model.sh
e2e_mesh_audit_forensics.sh
e2e_mesh_cache_retention.sh
e2e_mesh_emergency_disable.sh
e2e_mesh_remote_evidence.sh
e2e_mesh_surrogate_audit.sh
e2e_sandbox.sh
e2e_session_budget.sh

# --- scripts/e2e_overhaul/ — bd-1cn5o, recorded 2026-09-17 ---
#
# These 36 are NOT newly broken. They are newly VISIBLE. Until this commit the
# audit's population was scripts/e2e_*.sh alone (93 files) while
# scripts/e2e_overhaul/ held 115 more that the gate never looked at, so it was
# asserting "written tests actually execute" over less than half its subject
# and reporting "of 92 e2e scripts" as though that were the whole of it.
#
# Each has NO reference outside docs/ anywhere in the tree, by the same
# predicate applied to the other entries above. The inverse was checked before
# calling it an absence: nothing dispatches this directory generically --
# scripts/e2e_overhaul.sh runs 23 epics by explicit basename (EPIC_SCRIPTS,
# :55-79), and the only glob-shaped references in the tree are prose plus one
# path-class exclusion in e2e_event_contract_radar.sh:161.
#
# Watch scripts/e2e_overhaul/tiered_recall.sh against the registered
# tiered_recall_e2e.sh: a near-identical name one letter from a wired suite is
# precisely the miss this gate exists to catch.
#
# Baselined rather than fixed here for the reason the file's header gives:
# moving 36 scripts into the orphan set at once would wedge the gate for every
# agent, and triaging them is a decision, not a side effect of finally looking.
# THIS LIST MAY ONLY SHRINK — the stale arm above fails if any of these becomes
# wired and the line is left behind.
e2e_overhaul/agent_consumer.sh
e2e_overhaul/artifact_relocation.sh
e2e_overhaul/backup_derived.sh
e2e_overhaul/bayes_posteriors.sh
e2e_overhaul/cass_subprocess_supervision.sh
e2e_overhaul/completion_audit_multi_agent.sh
e2e_overhaul/consensus_conflict.sh
e2e_overhaul/coordination_snapshot.sh
e2e_overhaul/derivation_reflection_readiness.sh
e2e_overhaul/determinism_proptest.sh
e2e_overhaul/environment_attestation.sh
e2e_overhaul/hook_git_readiness.sh
e2e_overhaul/knowledge_gaps_recommendations.sh
e2e_overhaul/lint_determinism.sh
e2e_overhaul/mesh_command_modes.sh
e2e_overhaul/mi_dedup.sh
e2e_overhaul/networkx_differential.sh
e2e_overhaul/pack_binary.sh
e2e_overhaul/perf_explain_latency.sh
e2e_overhaul/perf_parity.sh
e2e_overhaul/prompt_budget.sh
e2e_overhaul/proof_broker_admission.sh
e2e_overhaul/regression_causality.sh
e2e_overhaul/singleflight_burst.sh
e2e_overhaul/submodular_resolution_b.sh
e2e_overhaul/swarm_incident_drill.sh
e2e_overhaul/sx1_stream.sh
e2e_overhaul/sx7_adaptive_budget.sh
e2e_overhaul/tiered_recall.sh
e2e_overhaul/verification_reuse.sh
e2e_overhaul/workspace_git_snapshot_read_only.sh

# ---------------------------------------------------------------------------
# Added 2026-09-19. THESE ARE NOT NEW ORPHANS. They were orphans all along and
# the audit could not see them, because two spellings of "a mention standing in
# for an invocation" still counted as invocation:
#
#   TEST FIXTURES ARE DATA. A JSON file that NAMES a script describes wiring
#   rather than performing it. cusum_maintenance.sh was reached only through a
#   fixture field literally called "invocation" -- a declaration of a thing
#   accepted as the thing.
#
#   A '//' COMMENT IS STILL A COMMENT. The rule rejected '#' only, so a Rust
#   doc line counted. e2e_rerank.sh was reached only by
#   src/core/search.rs:23188, "// exercised by scripts/e2e_rerank.sh, not unit
#   tests." -- a sentence saying a script provides the coverage, read as the
#   coverage.
#
# Baselined rather than wired for the reason at the top of this file: wiring
# nine suites at once would wedge the build. They are accepted debt, and the
# audit's shrink-only arm means this block can only get smaller.
#
# Reachable solely via tests/fixtures/*.json:
e2e_anchors_freshness.sh
e2e_br_concurrent_race.sh
e2e_contradiction.sh
e2e_error_recall.sh
e2e_gap_honesty.sh
e2e_overhaul/cusum_maintenance.sh
e2e_overhaul/swarm_fixture.sh
# Reachable solely via a '//' comment:
e2e_rerank.sh
e2e_overhaul/mesh_tailscale_smoke.sh
