# bd-c79fk — Part II surfaces that fail the tracing convention TODAY.
#
# Recorded 2026-09-16, the first time scripts/check-tracing-fields.sh was run
# against the tree by anything automated. Until this commit verify.sh ran only
# the checker's --self-test, so the real contract had never been evaluated by a
# gate and this debt accumulated unseen.
#
# Each line is an accepted, pre-existing violation. They were baselined rather
# than fixed because instrumenting seven production surfaces at once is a
# decision for the orchestrator, not a side effect of finally running the check.
#
# STARTED AT SEVEN; FIVE REMAIN, and the ratchet is what moved it:
#   7 -> 6  d499bc622  bd-3usjw.2  graph_centrality_read now passes
#   6 -> 5  f42dfd70a  bd-3usjw.1  db_inspect given a conformant event
# Four of the five carry no structured tracing evidence on any declared
# production surface; bd-3usjw.69 is missing its TRACING paragraph entirely.
# Each remaining line is named by its own bead, so the disposition of this debt
# is tracked by construction rather than by this file.
#
# This paragraph is prose over data, so it goes stale silently -- it described
# the original seven for two days after the list reached five. If you shrink the
# list, correct the count here in the same commit.
#
# THIS LIST MAY ONLY SHRINK. The audit fails in BOTH directions: a NEW violation
# is an error, and an entry here that now PASSES is also an error telling you to
# delete the line. The second arm is what stops a baseline decaying into a
# permanent ignore-list -- the failure mode this whole bead is about.
#
# Format: one bead id per line. Blank lines and # comments are ignored.

bd-3usjw.38
bd-3usjw.45
bd-3usjw.51
bd-3usjw.68
bd-3usjw.69
