# Context Pack: format before release

**embed_backend:** `hash_fallback`

**Profile:** compact | **Budget:** 8/4000 tokens

## Advisory Memory Banner

**Status:** `degraded`

Context includes 3 degraded signals; semantic embedding is unavailable, so treat retrieval ranking as lexical-only until a semantic model is available.

## procedural_rules

### 1. mem_00000000000000000000000001 (8 tokens)

```
Run cargo fmt --check before release.
```

**Why:** matched 'format before release' via lexical (relevance 0.5000, utility 0.8000)

**Trust:** `human_explicit` / `authoritative`

**Provenance:**
- `file://AGENTS.md#L164-173` (file)

## Degradations

- **[medium]** Proximity-to-seed scoring is disabled by graph.feature.proximity.enabled.
  - *Repair:* `ee config set graph.feature.proximity.enabled true`
- **[warning]** Embedding model unavailable (active embedder source frankensearch_hash_fallback reports semantic=false); semantic similarity is disabled and lexical search remains available.
  - *Repair:* `ee index reembed --workspace .`
- **[low]** Memory mem_00000000000000000000000001 evidence freshness is missing_source: Referenced provenance file \[REDACTED_PATH\] is missing.
  - *Repair:* `Restore the file or revise the memory provenance URI; rebuild the index if the memory content changes.`

---

*Generated by `ee pack "format before release" --format markdown`*

<!-- pack.hash: normalized-context-pack-hash -->
<!-- pack.schema: ee.response.v2 -->
