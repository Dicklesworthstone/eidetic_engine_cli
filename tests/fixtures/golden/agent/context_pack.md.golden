# Context Pack: format before release

**Profile:** compact | **Budget:** 8/4000 tokens

## Advisory Memory Banner

**Status:** `degraded`

Context includes 2 degraded signals; validate advisory memory and repair degraded sources before relying on this pack.

## procedural_rules

### 1. mem_00000000000000000000000001 (8 tokens)

```
Run cargo fmt --check before release.
```

**Why:** matched 'format before release' via hybrid (relevance 0.0328, utility 0.8000)

**Trust:** `human_explicit` / `authoritative`

**Provenance:**
- `file://AGENTS.md#L164-173` (file)

## Degradations

- **[low]** Memory mem_00000000000000000000000001 evidence freshness is missing_source: Referenced provenance file <ee-golden-workspace>/AGENTS.md is missing.
  - *Repair:* `Restore the file or revise the memory provenance URI; rebuild the index if the memory content changes.`
- **[low]** \`ee context\` is a compatibility alias for the promoted triad command.
  - *Repair:* ``Use `ee pack "<task>"`.``

---

*Generated by `ee context "format before release" --format markdown`*

<!-- pack.hash: normalized-context-pack-hash -->
<!-- pack.schema: ee.response.v1 -->
