# Context Pack: format before release

**Profile:** compact | **Budget:** 8/4000 tokens

## Advisory Memory Banner

**Status:** `clear`

Packed memories are from high\-trust classes; still verify provenance before acting\.

## procedural_rules

### 1. mem\_00000000000000000000000001 (8 tokens)

```
Run cargo fmt --check before release.
```

**Why:** Deterministic retrieval explanation for query \`format before release\`: source=memory search\_source=semantic\_fast; score\_components=\[relevance=unit\_score\(search\_hit\.score\) with search\_hit\.score=0\.4082, utility=unit\_score\(memory\.utility\) with memory\.utility=0\.8000\]; formula=unit\_score\(field\)=clamp\(field, 0\.0, 1\.0\) for finite fields, otherwise 0\.0; inputs are stored memory/link fields and the explicit search hit, not agent reasoning\.

**Trust:** `human_explicit` / `authoritative`

**Provenance:**
- `file://AGENTS.md#L164-173` (file)

---

*Generated by `ee context "format before release" --format markdown`*
