{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "hello_responder_rate_limited_storm",
  "introduced_by": {
    "bead": "bd-36bbk.1.12",
    "epic_letter": "SRR6.46"
  },
  "surfaces": [
    "mesh hello-responder status",
    "mesh hello handler"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "The responder has rejected many hello requests in the last hour because the per-peer 16 requests per 60 seconds limiter is firing repeatedly.",
    "setup_commands": [
      "simulate repeated hello requests from one peer until responder_busy is returned"
    ],
    "invocation": "ee mesh hello-responder status --workspace . --json"
  },
  "expected_emission": {
    "code": "hello_responder_rate_limited_storm",
    "severity": "warning",
    "message_contains": [
      "hello responder",
      "rate-limited"
    ],
    "repair_contains": "discovery policy"
  }
}
