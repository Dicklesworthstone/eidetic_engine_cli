{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "degraded_context",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "context"
  ],
  "severity": "medium",
  "repair_present": false,
  "trigger": {
    "description": "A context pack has one or more degraded retrieval or packing signals, so the advisory banner summarizes them.",
    "setup_commands": [
      "ee init --workspace .",
      "# trigger a context degradation such as stale index or unavailable graph features"
    ],
    "invocation": "ee context 'task' --workspace . --json"
  },
  "expected_emission": {
    "code": "degraded_context",
    "severity": "medium",
    "message_contains": [
      "degraded context signal",
      "inspect degraded"
    ]
  }
}
