{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "degraded_context",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "context"
  ],
  "severity": "info",
  "repair_present": false,
  "retired": true,
  "retired_by": {
    "bead": "bd-17c65.5.2",
    "reason": "The meta degraded_context banner duplicated concrete degraded[] entries and trained agents to ignore degraded output."
  },
  "trigger": {
    "description": "Retired legacy meta-code. Context now emits concrete degraded[] entries instead of a degraded_context summary banner.",
    "setup_commands": [
      "ee init --workspace .",
      "# confirm src/ has no production degraded_context emission pattern"
    ],
    "invocation": "scripts/e2e_overhaul/failure_modes.sh with EE_FAILURE_MODE_FILTER=degraded_context"
  },
  "expected_emission": {
    "code": "degraded_context",
    "severity": "info",
    "message_contains": [
      "degraded_context signal is retired",
      "inspect degraded[] concrete signals"
    ]
  }
}
