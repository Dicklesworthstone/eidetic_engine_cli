{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "embed_model_unavailable",
  "introduced_by": {
    "bead": "bd-3qs2i.4.1",
    "epic_letter": "F4"
  },
  "surfaces": [
    "search",
    "context"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "The embedding model or semantic embedder feature is unavailable while the lexical search arm remains functional. This includes explicit missing-model configuration and the default frankensearch_hash_fallback posture where the active embedder reports semantic=false.",
    "setup_commands": [
      "ee init --workspace .",
      "ee remember 'Cargo workspace uses Rust nightly.' --workspace . --level semantic --kind fact --json",
      "export EE_EMBED_MODEL_PATH=/nonexistent/model"
    ],
    "invocation": "EE_EMBED_MODEL_PATH=/nonexistent/model ee search 'cargo workspace' --workspace . --json"
  },
  "expected_emission": {
    "code": "embed_model_unavailable",
    "severity": "warning",
    "message_contains": [
      "Embedding model unavailable",
      "lexical"
    ],
    "repair_contains": "ee index reembed --workspace .",
    "repair_strings": [
      "ee index reembed --workspace .",
      "cargo build --features embed-quality"
    ]
  }
}
