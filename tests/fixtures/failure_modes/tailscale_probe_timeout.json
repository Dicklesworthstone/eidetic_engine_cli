{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "tailscale_probe_timeout",
  "introduced_by": {
    "bead": "bd-36bbk.1.1",
    "epic_letter": "SRR6.46"
  },
  "surfaces": [
    "status"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "Mesh status probing invokes a tailscale command that exceeds the configured timeout budget.",
    "setup_commands": [
      "source scripts/e2e_overhaul/lib/fake_tailscale.sh",
      "ft_init /tmp/ee-tailnet-timeout"
    ],
    "invocation": "EE_MESH_ENABLED=1 EE_TAILSCALE_PROBE_TIMEOUT_MS=1 ee status --workspace . --json"
  },
  "expected_emission": {
    "code": "tailscale_probe_timeout",
    "severity": "warning",
    "message_contains": [
      "Tailscale probe",
      "timeout"
    ],
    "repair_contains": "EE_TAILSCALE_PROBE_TIMEOUT_MS",
    "repair_string": "Run tailscale status directly or raise EE_TAILSCALE_PROBE_TIMEOUT_MS.",
    "repair_command_regex": "(?P<cmd>tailscale status)"
  }
}
