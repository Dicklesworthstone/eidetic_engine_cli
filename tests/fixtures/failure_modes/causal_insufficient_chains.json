{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "causal_insufficient_chains",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "causal compare"
  ],
  "severity": "info",
  "repair_present": false,
  "trigger": {
    "description": "Causal comparison filters match fewer than two chains.",
    "setup_commands": [
      "ee init --workspace .",
      "ee remember --workspace . --level episodic --kind failure 'J6 causal failure.' --json",
      "ee remember --workspace . --level episodic --kind root-cause 'J6 causal root cause.' --json",
      "ee diag causal-edge --edge-id cev_j6_insufficient_root --failure-id <failure-memory-id> --candidate-cause-id <root-memory-id> --workspace . --json"
    ],
    "invocation": "ee causal compare --artifact-id <root-memory-id> --workspace . --json"
  },
  "expected_emission": {
    "code": "causal_insufficient_chains",
    "severity": "info",
    "message_contains": [
      "comparison requires at least 2"
    ]
  }
}
