{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "causal_insufficient_chains",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "causal compare"
  ],
  "severity": "info",
  "repair_present": false,
  "trigger": {
    "description": "Causal comparison filters match fewer than two chains.",
    "setup_commands": [
      "ee init --workspace .",
      "# seed only one matching causal chain"
    ],
    "invocation": "ee causal compare --artifact-id artifact-1 --workspace . --json"
  },
  "expected_emission": {
    "code": "causal_insufficient_chains",
    "severity": "info",
    "message_contains": [
      "comparison requires at least 2"
    ]
  }
}
