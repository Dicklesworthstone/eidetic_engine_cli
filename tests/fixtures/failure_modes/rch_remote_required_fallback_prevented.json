{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "rch_remote_required_fallback_prevented",
  "introduced_by": {
    "bead": "bd-1zb7k.13.4",
    "epic_letter": "C"
  },
  "surfaces": [
    "swarm brief"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "Swarm brief collects RCH local capability while remote-only Cargo is required, but the current Codex shell has neither direct `rch exec` nor an installed Codex RCH hook.",
    "setup_commands": [
      "export RCH_REQUIRE_REMOTE=1",
      "# arrange RCH help/status captures where `exec` is unavailable and `agents status codex-cli --json` reports CodexCli as Not installed",
      "# run swarm brief --include-rch while no acceptable remote execution path is available"
    ],
    "invocation": "ee swarm brief --include-rch --workspace . --json"
  },
  "expected_emission": {
    "code": "rch_remote_required_fallback_prevented",
    "severity": "warning",
    "message_contains": [
      "Remote-only Cargo is required",
      "neither",
      "installed Codex RCH hook"
    ],
    "repair_contains": "harness with an installed RCH hook",
    "repair_string": "Use a harness with an installed RCH hook, upgrade RCH to expose `rch exec`, or record static-only evidence."
  }
}
