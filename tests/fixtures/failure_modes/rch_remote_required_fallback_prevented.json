{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "rch_remote_required_fallback_prevented",
  "introduced_by": {
    "bead": "bd-1zb7k.13.4",
    "epic_letter": "C"
  },
  "surfaces": [
    "swarm brief"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "Swarm brief collects RCH posture while remote-only execution is required and RCH refuses local fallback.",
    "setup_commands": [
      "export RCH_REQUIRE_REMOTE=1",
      "# run swarm brief --include-rch while no acceptable remote execution path is available"
    ],
    "invocation": "ee swarm brief --include-rch --workspace . --json"
  },
  "expected_emission": {
    "code": "rch_remote_required_fallback_prevented",
    "severity": "warning",
    "message_contains": [
      "RCH_REQUIRE_REMOTE",
      "no valid remote evidence"
    ],
    "repair_contains": "Fix remote worker availability",
    "repair_string": "Fix remote worker availability or unset the remote-required guard only with explicit approval."
  }
}
