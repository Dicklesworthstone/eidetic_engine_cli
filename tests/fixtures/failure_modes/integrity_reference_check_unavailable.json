{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "integrity_reference_check_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "diag integrity"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "The link and pack reference-integrity query itself fails.",
    "setup_commands": [
      "ee init --workspace .",
      "ee diag database-skew --workspace . --output-database ./integrity-reference-skew.db --skew memory-links-table-unavailable --json"
    ],
    "invocation": "ee diag integrity --database ./integrity-reference-skew.db --workspace . --json"
  },
  "expected_emission": {
    "code": "integrity_reference_check_unavailable",
    "severity": "medium",
    "message_contains": [
      "reference integrity checks",
      "could not be evaluated"
    ],
    "repair_contains": "ee diag integrity",
    "repair_string": "ee diag integrity --json",
    "repair_command_regex": "(?<cmd>ee [a-z][a-z0-9 -]*(?:--[a-z][a-z0-9-]*(?:[= ][^ ]+)?)*)"
  }
}
