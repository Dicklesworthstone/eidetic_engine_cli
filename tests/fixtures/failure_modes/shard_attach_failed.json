{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "shard_attach_failed",
  "introduced_by": {
    "bead": "bd-f6jfs.5",
    "epic_letter": "SHARD_FANOUT"
  },
  "surfaces": [
    "search",
    "context"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "Shard fan-out read planning included a peer workspace whose shard file could not be attached for a read-only cross-shard query.",
    "setup_commands": [
      "ee init --workspace . --json",
      "mkdir -p /tmp/ee-shard-root/shards",
      "touch /tmp/ee-shard-root/catalog.db"
    ],
    "invocation": "EE_SHARD_FANOUT_ENABLED=1 EE_SHARDS_DIR=/tmp/ee-shard-root/shards ee search 'release' --workspace . --memory-scope swarm --json"
  },
  "expected_emission": {
    "code": "shard_attach_failed",
    "severity": "warning",
    "message_contains": [
      "peer workspace shard",
      "read fan-out"
    ],
    "repair_contains": "ee migrate shard-fanout",
    "repair_string": "Verify the peer shard exists or run ee migrate shard-fanout --workspace . --dry-run --json.",
    "repair_command_regex": "(?<cmd>ee migrate shard-fanout --workspace \\. --dry-run --json)"
  }
}
