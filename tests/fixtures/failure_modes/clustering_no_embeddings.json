{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "clustering_no_embeddings",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "analyze clustering"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "Science clustering has a curation candidate whose target memory is missing and whose proposed content is empty, so there is no semantic payload to embed.",
    "setup_commands": [
      "ee init --workspace .",
      "ee diag curation-candidate --allow-missing-target --candidate-type rule --status pending --workspace . --json"
    ],
    "invocation": "ee analyze clustering --workspace . --candidate-type rule --status pending --json"
  },
  "expected_emission": {
    "code": "clustering_no_embeddings",
    "severity": "medium",
    "message_contains": [
      "Candidate embeddings not available"
    ],
    "repair_contains": "ee index rebuild"
  }
}
