{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "derived_asset_hash_mismatch",
  "introduced_by": {
    "bead": "bd-1nxz4.2",
    "epic_letter": "SRR6"
  },
  "surfaces": [
    "support bundle"
  ],
  "severity": "high",
  "repair_present": true,
  "trigger": {
    "description": "A content-addressed derived asset object's stored body does not match the BLAKE3 hash in its descriptor.",
    "setup_commands": [
      "ee support bundle --out <dir> --workspace . --json",
      "# inspect derivedAssetStore.degraded[] when a stored object body hash no longer matches metadata"
    ],
    "invocation": "ee support bundle --out <dir> --workspace . --json"
  },
  "expected_emission": {
    "code": "derived_asset_hash_mismatch",
    "severity": "high",
    "message_contains": [
      "derived asset object",
      "body hash"
    ],
    "repair_contains": "rebuild affected derived assets locally",
    "repair_string": "rebuild affected derived assets locally; do not reuse hash-mismatched objects"
  }
}
