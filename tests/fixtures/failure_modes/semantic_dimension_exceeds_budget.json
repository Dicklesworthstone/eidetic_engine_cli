{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "semantic_dimension_exceeds_budget",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "semantic model admissibility",
    "model status"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "A configured local embedding model exceeds the semantic dimension budget, so retrieval falls back to lexical mode.",
    "setup_commands": [
      "ee init --workspace .",
      "ee diag model-registry --workspace . --model-id mdl_j6000000000000000000000002 --provider custom --model-name j6-oversized-embedder --purpose embedding --dimension 4096 --distance-metric cosine --status available --version j6 --last-checked-at 2026-05-13T00:00:00Z --json"
    ],
    "invocation": "ee model status --workspace . --json"
  },
  "expected_emission": {
    "code": "semantic_dimension_exceeds_budget",
    "severity": "medium",
    "message_contains": [
      "dimension exceeds",
      "configured budget"
    ],
    "repair_contains": "select a smaller local embedding model",
    "repair_string": "select a smaller local embedding model or run `ee index reembed --workspace .`"
  }
}
