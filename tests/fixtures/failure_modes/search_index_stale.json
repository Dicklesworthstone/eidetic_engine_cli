{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "search_index_stale",
  "introduced_by": {
    "bead": "bd-17c65.2.1",
    "epic_letter": "B"
  },
  "surfaces": [
    "search",
    "context"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "The memory database generation is ahead of the search index generation, so search/context reads can miss recently-added memories until rebuild.",
    "setup_commands": [
      "ee init --workspace .",
      "ee remember 'Memory after old index generation.' --workspace . --level semantic --kind fact",
      "# Point search at an index directory whose meta.json generation is behind the database generation."
    ],
    "invocation": "ee search 'any query' --workspace . --json"
  },
  "expected_emission": {
    "code": "search_index_stale",
    "severity": "medium",
    "message_contains": [
      "Search index is stale",
      "index is rebuilt"
    ],
    "repair_contains": "ee index rebuild",
    "repair_string": "ee index rebuild --workspace .",
    "repair_command_regex": "(?<cmd>ee [a-z][a-z0-9 -]*(?:--[a-z][a-z0-9-]*(?:[= ][^ ]+)?)*)"
  }
}
