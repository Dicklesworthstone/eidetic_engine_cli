{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "hotset_retrieval_provenance_unavailable",
  "introduced_by": {
    "bead": "bd-ty3pl.2",
    "epic_letter": "T"
  },
  "surfaces": [
    "cache"
  ],
  "severity": "low",
  "repair_present": true,
  "trigger": {
    "description": "The selected workspace database is missing or unreadable, so the bounded in-process collector abstains from recent pack/search provenance.",
    "setup_commands": [
      "mkdir -p <uninitialized-workspace>"
    ],
    "invocation": "ee cache hotset-manifest --workspace <uninitialized-workspace> --json"
  },
  "expected_emission": {
    "code": "hotset_retrieval_provenance_unavailable",
    "severity": "low",
    "message_contains": [
      "workspace database is missing or unreadable"
    ]
  }
}
