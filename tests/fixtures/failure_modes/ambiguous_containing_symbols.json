{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "ambiguous_containing_symbols",
  "introduced_by": {
    "bead": "bd-2xuu7.6",
    "epic_letter": "G"
  },
  "surfaces": [
    "symbol evidence links",
    "context",
    "why"
  ],
  "severity": "warning",
  "repair_present": false,
  "trigger": {
    "description": "Symbol evidence points at a source span contained by multiple equally-specific symbols, so the link resolver refuses to guess which symbol owns the evidence.",
    "setup_commands": [
      "ee init --workspace .",
      "printf 'mod a { pub fn run() {} } mod b { pub fn run() {} }\\n' > src/lib.rs",
      "ee symbol snapshot --workspace . --json",
      "ee symbol link --workspace . --evidence tests/fixtures/symbol_graph/ambiguous_span.json --json"
    ],
    "invocation": "ee symbol link --workspace . --evidence ambiguous-span.json --json"
  },
  "expected_emission": {
    "code": "ambiguous_containing_symbols",
    "severity": "warning",
    "message_contains": [
      "multiple symbols matched",
      "equal specificity"
    ]
  }
}
