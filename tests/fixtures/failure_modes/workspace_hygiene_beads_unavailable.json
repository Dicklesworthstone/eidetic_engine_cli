{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "workspace_hygiene_beads_unavailable",
  "introduced_by": {
    "bead": "bd-1eq3l.11",
    "epic_letter": "WH"
  },
  "surfaces": [
    "workspace hygiene"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "Workspace hygiene cannot collect Beads status, assignment, dependency, or export-divergence metadata.",
    "setup_commands": [
      "# run workspace hygiene where br is unavailable or .beads cannot be read"
    ],
    "invocation": "ee workspace hygiene --workspace . --json"
  },
  "expected_emission": {
    "code": "workspace_hygiene_beads_unavailable",
    "severity": "medium",
    "message_contains": [
      "Beads"
    ],
    "repair_contains": "br",
    "repair_strings": [
      "Run `br ready --json` in the workspace.",
      "Run `br doctor` if Beads metadata cannot be read."
    ],
    "repair_command_regex": "Run `(?<cmd>br (ready --json|doctor))`"
  }
}
