{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "tailscale_peer_probe_timeout",
  "introduced_by": {
    "bead": "bd-36bbk.1.2",
    "epic_letter": "SRR6.46"
  },
  "surfaces": [
    "mesh status"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "At least one policy-eligible tailnet peer does not answer the ee hello probe within the per-peer timeout.",
    "setup_commands": [
      "source scripts/e2e_overhaul/lib/fake_tailscale.sh",
      "ft_init /tmp/ee-tailnet-peer-timeout",
      "ft_add_peer nodekey:peer 100.64.0.20 peer --tag=tag:ee-mesh --ee_version=0.2.0 --ee_protocol=1.0 --workspace_ids=workspace-alpha --latency_ms=1000"
    ],
    "invocation": "EE_MESH_ENABLED=1 EE_TAILSCALE_PEER_PROBE_TIMEOUT_MS=1 ee mesh status --workspace . --json"
  },
  "expected_emission": {
    "code": "tailscale_peer_probe_timeout",
    "severity": "warning",
    "message_contains": [
      "hello probe",
      "budget"
    ],
    "repair_contains": "EE_TAILSCALE_PEER_PROBE_TIMEOUT_MS"
  }
}
