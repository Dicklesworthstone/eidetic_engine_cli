{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "mesh_peer_network_only_denied",
  "introduced_by": {
    "bead": "bd-1x87h",
    "epic_letter": "SRR6.24"
  },
  "surfaces": [
    "mesh peer add"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "The handshake's responder node key does not match the endpoint's Tailscale node key, so enrollment is denied: a reachable Tailscale node is never sufficient by itself to authorize a peer.",
    "setup_commands": [
      "ee init --workspace . --json",
      "supply a handshake node key that differs from the endpoint node key"
    ],
    "invocation": "ee mesh peer add <node-key> --yes --handshake-node-key <other-key> --workspace . --json"
  },
  "expected_emission": {
    "code": "mesh_peer_network_only_denied",
    "severity": "medium",
    "message_contains": [
      "node"
    ],
    "repair_contains": "ee mesh"
  }
}
