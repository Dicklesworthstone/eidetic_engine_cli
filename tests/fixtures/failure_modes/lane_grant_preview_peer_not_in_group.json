{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "lane_grant_preview_peer_not_in_group",
  "introduced_by": {
    "bead": "bd-36bbk.1.17",
    "epic_letter": "SRR6.46.17"
  },
  "surfaces": [
    "mesh preview-grant"
  ],
  "severity": "info",
  "repair_present": true,
  "trigger": {
    "description": "A lane-grant preview targets an enrolled opaque peer ID that is not currently named by the workspace's peer-group bindings, so the preview warns that materializing the grant would also require group membership.",
    "setup_commands": [
      "ee init --workspace . --json",
      "enroll/configure peer_example but omit it from [[mesh.peer_group_bindings]]",
      "run preview-grant against the enrolled opaque peer ID"
    ],
    "invocation": "ee mesh preview-grant peer_example --lane metadata --workspace . --json"
  },
  "expected_emission": {
    "code": "lane_grant_preview_peer_not_in_group",
    "severity": "info",
    "message_contains": [
      "peer"
    ],
    "repair_contains": "ee mesh"
  }
}
