{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "mcp_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "diag dependencies",
    "dependency contract"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "The optional MCP adapter is unavailable or not compiled; normal CLI operation remains available.",
    "setup_commands": [
      "ee diag dependencies --workspace . --json"
    ],
    "invocation": "ee diag dependencies --workspace . --json"
  },
  "expected_emission": {
    "code": "mcp_unavailable",
    "severity": "medium",
    "message_contains": [
      "MCP",
      "unavailable"
    ],
    "repair_contains": "mcp"
  }
}
