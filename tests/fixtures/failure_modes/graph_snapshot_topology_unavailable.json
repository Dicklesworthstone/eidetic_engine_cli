{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "graph_snapshot_topology_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "graph export"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "A graph snapshot cannot be exported because its metrics JSON has no exportable topology.",
    "setup_commands": [
      "ee init --workspace .",
      "# insert a graph snapshot whose metrics JSON omits nodes or edges"
    ],
    "invocation": "ee graph export --workspace . --json"
  },
  "expected_emission": {
    "code": "graph_snapshot_topology_unavailable",
    "severity": "medium",
    "message_contains": [
      "topology"
    ],
    "repair_contains": "Regenerate a graph snapshot"
  }
}
