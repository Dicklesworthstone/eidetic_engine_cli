{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "host_calibration_contradictory",
  "introduced_by": {
    "bead": "bd-1zb7k.12.3.4",
    "epic_letter": "H"
  },
  "surfaces": ["profile config plan", "host calibration recommendation"],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "Host calibration evidence contains contradictory live results, so the recommender caps high-resource profiles.",
    "setup_commands": [],
    "invocation": "ee profile config plan --workspace . --json"
  },
  "expected_emission": {
    "code": "host_calibration_contradictory",
    "severity": "medium",
    "message_contains": ["Calibration evidence is contradictory"],
    "repair_contains": "ee diag host-profile --workspace . --json"
  }
}
