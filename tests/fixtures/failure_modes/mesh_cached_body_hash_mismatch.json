{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "mesh_cached_body_hash_mismatch",
  "introduced_by": {
    "bead": "bd-nw0v3.3",
    "epic_letter": "SRR6.16"
  },
  "surfaces": [
    "mesh cache",
    "context",
    "mesh status",
    "doctor"
  ],
  "severity": "high",
  "repair_present": true,
  "trigger": {
    "description": "A cached peer body exists locally, but its recorded local body hash differs from the expected remote content hash, so ee quarantines the cached body and reports the mismatch instead of using potentially corrupted or stale peer content.",
    "setup_commands": [
      "ee init --workspace . --json",
      "ee mesh import --workspace . --file peer-body-cached-mismatch.json --json",
      "ee mesh cache verify --workspace . --body-cache-key body-cache-alpha --json"
    ],
    "invocation": "ee context 'peer body evidence' --workspace . --memory-scope swarm --json"
  },
  "expected_emission": {
    "code": "mesh_cached_body_hash_mismatch",
    "severity": "high",
    "message_contains": [
      "cached body",
      "hash",
      "mismatch"
    ],
    "repair_contains": "Quarantine",
    "repair_string": "Quarantine the cached peer body, re-sync from the peer, and verify the advertised content hash before using body material."
  }
}
