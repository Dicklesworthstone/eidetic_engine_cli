{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "hotset_git_unavailable",
  "introduced_by": {
    "bead": "bd-ty3pl.2",
    "epic_letter": "T"
  },
  "surfaces": [
    "cache"
  ],
  "severity": "low",
  "repair_present": true,
  "trigger": {
    "description": "The bounded git status probe cannot run (spawn failure or nonzero exit).",
    "setup_commands": [
      "ee cache hotset-manifest --git-program /nonexistent/git --json"
    ],
    "invocation": "ee cache hotset-manifest --workspace . --json"
  },
  "expected_emission": {
    "code": "hotset_git_unavailable",
    "severity": "low",
    "message_contains": [
      "git status probe could not run"
    ]
  }
}
