{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "integrity_schema_check_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "diag integrity"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "Integrity diagnostics cannot inspect the migration state of the opened database.",
    "setup_commands": [
      "ee init --workspace .",
      "# use a fixture database whose migration metadata query fails"
    ],
    "invocation": "ee diag integrity --workspace . --json"
  },
  "expected_emission": {
    "code": "integrity_schema_check_unavailable",
    "severity": "medium",
    "message_contains": [
      "migration state",
      "could not be inspected"
    ],
    "repair_contains": "ee doctor"
  }
}
