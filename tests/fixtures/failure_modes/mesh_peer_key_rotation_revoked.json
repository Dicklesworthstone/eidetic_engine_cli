{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "mesh_peer_key_rotation_revoked",
  "introduced_by": {
    "bead": "bd-1x87h",
    "epic_letter": "SRR6.24"
  },
  "surfaces": [
    "mesh peer rotate"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "A key rotation is attempted for a peer whose record is revoked; rotation is denied because a revoked peer must be re-enrolled through the explicit consent path, not resurrected by rotation.",
    "setup_commands": [
      "enroll a mesh peer",
      "revoke it with ee mesh peer revoke",
      "attempt a rotation on the revoked record"
    ],
    "invocation": "ee mesh peer rotate <revoked-node-key> --fingerprint <new> --workspace . --json"
  },
  "expected_emission": {
    "code": "mesh_peer_key_rotation_revoked",
    "severity": "medium",
    "message_contains": [
      "revoked"
    ],
    "repair_contains": "ee mesh peer add"
  }
}
