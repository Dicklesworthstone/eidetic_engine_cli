{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "sprt_quarantine",
  "introduced_by": {
    "bead": "bd-3qs2i.3.1",
    "epic_letter": "F"
  },
  "surfaces": [
    "outcome"
  ],
  "severity": "warning",
  "repair_present": false,
  "trigger": {
    "description": "A harmful outcome event from a source whose recent helpful/harmful sequence crosses the SPRT bad-source threshold is absorbed into feedback quarantine instead of live scoring.",
    "setup_commands": [
      "ee init --workspace . --json",
      "ee remember 'Victim memory' --workspace . --level procedural --kind rule --json",
      "ee outcome <memory-id> --signal harmful --source-id sprt-source --reason 'warmup 1' --harmful-per-source-per-hour 100 --workspace . --json",
      "ee outcome <memory-id> --signal harmful --source-id sprt-source --reason 'warmup 2' --harmful-per-source-per-hour 100 --workspace . --json",
      "ee outcome <memory-id> --signal harmful --source-id sprt-source --reason 'warmup 3' --harmful-per-source-per-hour 100 --workspace . --json"
    ],
    "invocation": "ee outcome <memory-id> --signal harmful --source-id sprt-source --reason 'cross SPRT quarantine threshold' --harmful-per-source-per-hour 100 --workspace . --json"
  },
  "expected_emission": {
    "code": "sprt_quarantine",
    "severity": "warning",
    "message_contains": [
      "SPRT outcome quarantine threshold exceeded",
      "event was quarantined",
      "did NOT update live scoring"
    ],
    "details_contains": [
      "sourceId",
      "classifiedEventCount",
      "quarantinedCandidateIds",
      "reason",
      "recovery"
    ],
    "recovery_kinds": [
      "command",
      "narrow"
    ]
  }
}
