{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "snapshot_release_failed",
  "introduced_by": {
    "bead": "bd-2caru.6",
    "epic_letter": "SRR"
  },
  "surfaces": [
    "context",
    "status"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "A SnapshotPin cleanup path cannot commit or roll back the read transaction, so ee abandons the pooled connection instead of returning it to the idle pool.",
    "setup_commands": [
      "start a context/read-pool workflow with snapshot pinning enabled",
      "inject a rollback or commit failure before the SnapshotPin is dropped"
    ],
    "invocation": "ee status --workspace . --json"
  },
  "expected_emission": {
    "code": "snapshot_release_failed",
    "severity": "medium",
    "message_contains": [
      "snapshot",
      "release failed"
    ],
    "repair_contains": "ee doctor"
  }
}
