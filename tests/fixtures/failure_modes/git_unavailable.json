{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "git_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "swarm brief",
    "workspace hygiene"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "A coordination or workspace-hygiene surface cannot collect git state for the workspace.",
    "setup_commands": [
      "# run swarm brief outside a git checkout or with git unavailable"
    ],
    "invocation": "ee swarm brief --workspace . --json"
  },
  "expected_emission": {
    "code": "git_unavailable",
    "severity": "warning",
    "message_contains": [
      "git was not found"
    ],
    "repair_contains": "git",
    "repair_strings": [
      "Run `git status --short` in the workspace.",
      "Run `git log -n 8 --format=%H%x1f%ct%x1f%s` in the workspace."
    ],
    "repair_command_regex": "Run `(?<cmd>git [^`]+)`"
  }
}
