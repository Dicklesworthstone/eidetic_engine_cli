{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "graph_projection_failed",
  "introduced_by": {
    "bead": "bd-1idcb",
    "epic_letter": "G"
  },
  "surfaces": [
    "focus suggest"
  ],
  "severity": "warning",
  "repair_present": false,
  "trigger": {
    "description": "`ee focus suggest` failed to build the memory graph projection — `build_memory_graph` returned an error (storage failure reading `memory_links`, malformed edge data, runtime cancellation, etc.). The pipeline falls back to recency-only ranking and emits this degraded entry so the caller knows centrality contributions are unavailable.",
    "setup_commands": [
      "ee init --workspace ."
    ],
    "invocation": "ee focus suggest --workspace . --json"
  },
  "expected_emission": {
    "code": "graph_projection_failed",
    "severity": "warning",
    "message_contains": [
      "Failed to build memory graph projection",
      "recency-only scoring"
    ]
  }
}
