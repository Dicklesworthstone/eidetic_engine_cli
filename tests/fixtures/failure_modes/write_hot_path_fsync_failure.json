{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "write_hot_path_fsync_failure",
  "introduced_by": {
    "bead": "bd-2lsxf.2.4",
    "epic_letter": "SRR3"
  },
  "surfaces": [
    "write hot path fake runner"
  ],
  "severity": "high",
  "repair_present": true,
  "trigger": {
    "description": "The SRR3 fake runner executes a deterministic schedule whose modeled fsync failure must leave the affected batch non-durable and must not publish a snapshot.",
    "setup_commands": [
      "ee init --workspace ."
    ],
    "invocation": "cargo test --lib srr3_fake_runner_rejects_partial_fsync_publication -- --nocapture"
  },
  "expected_emission": {
    "code": "write_hot_path_fsync_failure",
    "severity": "high",
    "message_contains": [
      "fsync failure",
      "audit-chain digest"
    ],
    "repair_contains": "inspect the SRR3 fake-runner event log"
  }
}
