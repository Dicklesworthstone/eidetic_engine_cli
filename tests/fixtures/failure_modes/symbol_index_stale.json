{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "symbol_index_stale",
  "introduced_by": {
    "bead": "bd-2xuu7.6",
    "epic_letter": "G"
  },
  "surfaces": [
    "symbol snapshot",
    "context",
    "why"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "A persisted symbol snapshot was generated from an older source manifest than the current workspace, so symbol-boosted retrieval may lag source edits.",
    "setup_commands": [
      "ee init --workspace .",
      "ee symbol snapshot --workspace . --json",
      "printf 'pub fn changed() {}\\n' >> src/lib.rs"
    ],
    "invocation": "ee context 'changed symbol' --workspace . --json"
  },
  "expected_emission": {
    "code": "symbol_index_stale",
    "severity": "warning",
    "message_contains": [
      "Symbol index is stale",
      "snapshot"
    ],
    "repair_contains": "ee symbol snapshot",
    "repair_string": "ee symbol snapshot --workspace . --refresh",
    "repair_command_regex": "(?<cmd>ee [a-z][a-z0-9 -]*(?:--[a-z][a-z0-9-]*(?:[= ][^ ]+)?)*)"
  }
}
