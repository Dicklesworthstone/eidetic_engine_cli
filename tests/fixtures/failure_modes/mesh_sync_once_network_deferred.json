{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "mesh_sync_once_network_deferred",
  "introduced_by": {
    "bead": "bd-36bbk.2",
    "epic_letter": "SRR6.8"
  },
  "surfaces": [
    "mesh sync"
  ],
  "severity": "info",
  "repair_present": true,
  "trigger": {
    "description": "Foreground `mesh sync --once` runs while no usable foreground peer transport path is available (the production default until the team-confederation transport lands), so no peer is contacted and the honest deferral posture is reported.",
    "setup_commands": [
      "ee init --workspace . --json",
      "enable mesh with EE_MESH_ENABLED=1 and leave the default no-transport sync path in place"
    ],
    "invocation": "ee mesh sync --once --workspace . --json"
  },
  "expected_emission": {
    "code": "mesh_sync_once_network_deferred",
    "severity": "info",
    "message_contains": [
      "did not contact peers",
      "no usable foreground peer transport"
    ],
    "repair_contains": "Inspect enrolled peers"
  }
}
