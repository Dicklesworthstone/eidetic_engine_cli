{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "shard_chain_mismatch",
  "introduced_by": {
    "bead": "bd-f6jfs.6",
    "epic_letter": "SHARD_FANOUT"
  },
  "surfaces": [
    "audit verify",
    "audit timeline"
  ],
  "severity": "high",
  "repair_present": true,
  "trigger": {
    "description": "One shard-local audit chain points at an unexpected previous hash while other shard chains remain independently verifiable.",
    "setup_commands": [
      "ee init --workspace . --json",
      "ee migrate shard-fanout --workspace . --json"
    ],
    "invocation": "ee audit verify --workspace . --json"
  },
  "expected_emission": {
    "code": "shard_chain_mismatch",
    "severity": "high",
    "message_contains": [
      "shard",
      "audit chain",
      "mismatch"
    ],
    "repair_contains": "ee audit timeline",
    "repair_string": "Inspect ee audit timeline --workspace . --json for the shard_id reported by ee audit verify, then restore or quarantine the broken shard before trusting cross-shard audit evidence.",
    "repair_command_regex": "(?<cmd>ee audit timeline --workspace \\. --json)"
  }
}
