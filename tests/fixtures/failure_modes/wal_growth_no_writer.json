{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "wal_growth_no_writer",
  "introduced_by": {
    "bead": "bd-2caru.8",
    "epic_letter": "READ_POOL"
  },
  "surfaces": [
    "status",
    "doctor"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "A pure-read deployment observes WAL growth beyond threshold but no local checkpoint writer or daemon path is active.",
    "setup_commands": [
      "ee init --workspace . --json",
      "EE_WAL_CHECKPOINT_BYTES_THRESHOLD=1 ee status --workspace . --json"
    ],
    "invocation": "EE_WAL_CHECKPOINT_BYTES_THRESHOLD=1 ee status --workspace . --json"
  },
  "expected_emission": {
    "code": "wal_growth_no_writer",
    "severity": "medium",
    "message_contains": [
      "WAL growth",
      "checkpoint writer"
    ],
    "repair_contains": "ee maintenance wal-checkpoint",
    "repair_string": "Run `ee maintenance wal-checkpoint --workspace .`.",
    "repair_command_regex": "(?<cmd>ee [a-z][a-z0-9 -]*(?:--[a-z][a-z0-9-]*(?:[= ][^ ]+)?)*)"
  }
}
