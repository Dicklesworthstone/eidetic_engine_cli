{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "host_calibration_partial",
  "introduced_by": {
    "bead": "bd-1zb7k.12.3.4",
    "epic_letter": "H"
  },
  "surfaces": ["profile config plan", "host calibration recommendation"],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "Host calibration evidence is present but incomplete, so the recommender caps high-resource profiles.",
    "setup_commands": [],
    "invocation": "ee profile config plan --workspace . --json"
  },
  "expected_emission": {
    "code": "host_calibration_partial",
    "severity": "warning",
    "message_contains": ["Host calibration evidence is partial"],
    "repair_contains": "rch exec -- scripts/e2e_overhaul/host_calibration.sh"
  }
}
