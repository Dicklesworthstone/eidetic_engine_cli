{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "conformal_calibration_insufficient",
  "introduced_by": { "bead": "bd-17c65.14.2", "epic_letter": "N" },
  "surfaces": ["search", "diag search"],
  "severity": "low",
  "repair_present": true,
  "trigger": {
    "description": "A workspace has .ee/search/calibration.jsonl, but it contains fewer than the minimum usable outcome-backed relevance samples needed for split-conformal score intervals.",
    "setup_commands": [
      "ee init --workspace .",
      "mkdir -p .ee/search",
      "printf '%s\\n' '{\"score\":0.8,\"groundTruthRelevance\":0.6}' > .ee/search/calibration.jsonl",
      "ee remember 'Run cargo fmt before release.' --workspace . --level procedural --kind rule"
    ],
    "invocation": "ee search 'format before release' --workspace . --json"
  },
  "expected_emission": {
    "code": "conformal_calibration_insufficient",
    "severity": "low",
    "message_contains": [
      "Search score calibration",
      "required for split-conformal score intervals"
    ],
    "repair_contains": ".ee/search/calibration.jsonl"
  }
}
