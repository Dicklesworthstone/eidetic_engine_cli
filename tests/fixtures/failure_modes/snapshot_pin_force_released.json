{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "snapshot_pin_force_released",
  "introduced_by": {
    "bead": "bd-2caru.6",
    "epic_letter": "SRR"
  },
  "surfaces": [
    "status",
    "workspace close"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "Workspace close waits for active read pins up to its drain timeout, then force-poisons any remaining SnapshotPin so writer shutdown can complete without leaking WAL readers.",
    "setup_commands": [
      "start a context/read-pool workflow that holds a SnapshotPin",
      "close the workspace while the pin remains active past EE_WORKSPACE_CLOSE_DRAIN_TIMEOUT_S"
    ],
    "invocation": "ee status --workspace . --json"
  },
  "expected_emission": {
    "code": "snapshot_pin_force_released",
    "severity": "medium",
    "message_contains": [
      "snapshot pin",
      "force released"
    ],
    "repair_contains": "retry"
  }
}
