{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "scope_agent_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "search",
    "context"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "A self or team scoped retrieval is requested without EE_AGENT_NAME, so the current agent identity cannot be resolved.",
    "setup_commands": [
      "ee init --workspace . --json",
      "ee remember --workspace . --level semantic --kind fact 'Scoped memory fixture.' --json",
      "ee index rebuild --workspace . --json",
      "unset EE_AGENT_NAME"
    ],
    "invocation": "ee search 'Scoped memory fixture' --workspace . --memory-scope self --json"
  },
  "expected_emission": {
    "code": "scope_agent_unavailable",
    "severity": "warning",
    "message_contains": [
      "current agent identity",
      "EE_AGENT_NAME"
    ],
    "repair_contains": "EE_AGENT_NAME",
    "repair_string": "Set EE_AGENT_NAME for self/team scoped retrieval."
  }
}
