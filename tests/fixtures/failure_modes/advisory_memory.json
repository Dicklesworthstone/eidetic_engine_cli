{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "advisory_memory",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "context"
  ],
  "severity": "medium",
  "repair_present": false,
  "trigger": {
    "description": "A context pack includes memories whose trust posture is advisory rather than validated policy.",
    "setup_commands": [
      "ee init --workspace .",
      "ee import jsonl --workspace . --source advisory-memory.jsonl --json"
    ],
    "invocation": "ee pack 'J6 advisory imported memory marker.' --workspace . --json"
  },
  "expected_emission": {
    "code": "advisory_memory",
    "severity": "medium",
    "message_contains": [
      "agent assertions",
      "validated against provenance"
    ]
  }
}
