{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "hotset_source_authority_missing",
  "introduced_by": {
    "bead": "bd-ty3pl.2",
    "epic_letter": "T"
  },
  "surfaces": [
    "cache"
  ],
  "severity": "low",
  "repair_present": true,
  "trigger": {
    "description": "No source-authority snapshot is readable at the resolved path; verification posture cannot inform the hotset.",
    "setup_commands": [
      "rm -f <workspace>/.ee/source-authority-snapshot.json"
    ],
    "invocation": "ee cache hotset-manifest --workspace . --json"
  },
  "expected_emission": {
    "code": "hotset_source_authority_missing",
    "severity": "low",
    "message_contains": [
      "source-authority snapshot"
    ]
  }
}
