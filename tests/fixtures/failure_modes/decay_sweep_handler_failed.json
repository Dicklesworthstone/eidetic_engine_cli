{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "decay_sweep_handler_failed",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "job run"
  ],
  "severity": "high",
  "repair_present": false,
  "trigger": {
    "description": "The decay sweep job reaches the score-decay handler, but the handler returns an error.",
    "setup_commands": [
      "ee init --workspace .",
      "ee diag database-skew --workspace . --output-database ./decay-handler-skew.db --skew decay-memories-table-unavailable --json"
    ],
    "invocation": "ee job run decay_sweep --database ./decay-handler-skew.db --workspace . --json"
  },
  "expected_emission": {
    "code": "decay_sweep_handler_failed",
    "severity": "high",
    "message_contains": [
      "decay"
    ]
  }
}
