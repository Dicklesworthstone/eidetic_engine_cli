{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "decay_sweep_handler_failed",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "job run"
  ],
  "severity": "high",
  "repair_present": false,
  "trigger": {
    "description": "The decay sweep job reaches the score-decay handler, but the handler returns an error.",
    "setup_commands": [
      "ee init --workspace .",
      "# use a fixture that makes run_score_decay_job fail during preflight or mutation"
    ],
    "invocation": "ee job run decay_sweep --workspace . --json"
  },
  "expected_emission": {
    "code": "decay_sweep_handler_failed",
    "severity": "high",
    "message_contains": [
      "decay"
    ]
  }
}
