{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "mesh_frame_replay_rejected",
  "introduced_by": { "bead": "bd-tc-epic-qzk7o.3.2", "epic_letter": "TC" },
  "surfaces": ["mesh authenticated transport"],
  "severity": "high",
  "repair_present": true,
  "trigger": {
    "description": "A frame counter is duplicated, skipped, regressed, or reused after the checked u64::MAX terminal counter.",
    "setup_commands": ["send a validly MACed frame whose directional counter is not exactly next"],
    "invocation": "exchange an authenticated mesh transport frame"
  },
  "expected_emission": {
    "code": "mesh_frame_replay_rejected",
    "severity": "high",
    "message_contains": ["counter", "session closes"],
    "repair_contains": "open a fresh authenticated session"
  }
}
