{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "proof_tool_missing",
  "introduced_by": {
    "bead": "bd-nnfq4",
    "epic_letter": "SRR2"
  },
  "surfaces": [
    "verify proofs"
  ],
  "severity": "info",
  "repair_present": true,
  "trigger": {
    "description": "The proof verification surface discovers Lean4 or TLA+ proof artifacts but the required external proof tool is not available on PATH.",
    "setup_commands": [
      "ee init --workspace .",
      "env PATH=/usr/bin:/bin ee verify proofs --workspace . --json"
    ],
    "invocation": "ee verify proofs --workspace . --json"
  },
  "expected_emission": {
    "code": "proof_tool_missing",
    "severity": "info",
    "message_contains": [
      "proof",
      "tool",
      "missing"
    ],
    "repair_contains": "Install Lean4 lake or TLA+ TLC",
    "repair_string": "Install Lean4 lake or TLA+ TLC, then rerun ee verify proofs --json.",
    "repair_command_regex": "(?P<cmd>ee verify proofs --json)"
  }
}
