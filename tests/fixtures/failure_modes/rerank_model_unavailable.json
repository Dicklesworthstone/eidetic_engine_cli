{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "rerank_model_unavailable",
  "introduced_by": {
    "bead": "bd-2vq2z.6",
    "epic_letter": "RERANK"
  },
  "surfaces": [
    "search"
  ],
  "severity": "low",
  "repair_present": true,
  "trigger": {
    "description": "The search pipeline is in rerank auto mode, semantic retrieval is available, but the workspace has no available local cross-encoder reranker registry entry.",
    "setup_commands": [
      "ee init --workspace .",
      "ee remember 'Rerank fixture memory about release verification.' --workspace . --level semantic --kind fact --json"
    ],
    "invocation": "ee search 'release verification' --workspace . --json"
  },
  "expected_emission": {
    "code": "rerank_model_unavailable",
    "severity": "low",
    "message_contains": [
      "No usable local reranker is registered",
      "fusion-only ranking",
      "verified offline reranker artifact"
    ],
    "repair_contains": "ee model fetch rerank-default",
    "repair_string": "ee model fetch rerank-default --workspace . --from-file /path/to/rerank-default-v1.tar.zst"
  }
}
