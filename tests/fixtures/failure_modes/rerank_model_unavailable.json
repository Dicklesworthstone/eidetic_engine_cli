{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "rerank_model_unavailable",
  "introduced_by": {
    "bead": "bd-2vq2z.6",
    "epic_letter": "RERANK"
  },
  "surfaces": [
    "search"
  ],
  "severity": "low",
  "repair_present": true,
  "trigger": {
    "description": "The search pipeline is in rerank auto mode, semantic retrieval is available, but no loadable local cross-encoder reranker can be resolved from the workspace model registry/cache.",
    "setup_commands": [
      "ee init --workspace .",
      "ee remember 'Rerank fixture memory about release verification.' --workspace . --level semantic --kind fact --json"
    ],
    "invocation": "ee search 'release verification' --workspace . --json"
  },
  "expected_emission": {
    "code": "rerank_model_unavailable",
    "severity": "low",
    "message_contains": [
      "Search rerank is in auto mode",
      "fusion-only ranking"
    ],
    "repair_contains": "ee model fetch rerank-default",
    "repair_string": "ee model fetch rerank-default --from-file /path/to/rerank-default-v1.tar.zst",
    "repair_command_regex": "(?<cmd>ee [a-z][a-z0-9 -]*(?:--[a-z][a-z0-9-]*(?:[= ][^ ]+)?)*)"
  }
}
