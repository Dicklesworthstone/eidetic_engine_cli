{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "tailscale_shields_up",
  "introduced_by": {
    "bead": "bd-36bbk.1.1",
    "epic_letter": "SRR6.46"
  },
  "surfaces": [
    "status"
  ],
  "severity": "warning",
  "repair_present": true,
  "trigger": {
    "description": "Mesh status probing reaches an authenticated tailscaled node whose prefs report ShieldsUp=true.",
    "setup_commands": [
      "source scripts/e2e_overhaul/lib/fake_tailscale.sh",
      "ft_init /tmp/ee-tailnet-shields-up",
      "ft_set_prefs --shields-up true"
    ],
    "invocation": "EE_MESH_ENABLED=1 ee status --workspace . --json"
  },
  "expected_emission": {
    "code": "tailscale_shields_up",
    "severity": "warning",
    "message_contains": [
      "shields-up",
      "peers cannot initiate discovery"
    ],
    "repair_contains": "tailscale set --shields-up=false",
    "repair_string": "Run tailscale set --shields-up=false if you want symmetric mesh discovery.",
    "repair_command_regex": "(?P<cmd>tailscale set --shields-up=false)"
  }
}
