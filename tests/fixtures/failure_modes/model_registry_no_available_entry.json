{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "model_registry_no_available_entry",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "model status",
    "model list"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "The model registry contains entries, but every entry is disabled or unavailable.",
    "setup_commands": [
      "ee init --workspace .",
      "ee diag model-registry --workspace . --model-id mdl_j6000000000000000000000001 --provider hash --model-name j6-disabled-hash --purpose embedding --dimension 256 --distance-metric cosine --status disabled --version j6 --last-checked-at 2026-05-13T00:00:00Z --json"
    ],
    "invocation": "ee model status --workspace . --json"
  },
  "expected_emission": {
    "code": "model_registry_no_available_entry",
    "severity": "medium",
    "message_contains": [
      "none are marked available",
      "semantic search is degraded"
    ],
    "repair_contains": "ee doctor",
    "repair_string": "ee doctor --json",
    "repair_command_regex": "(?<cmd>ee [a-z][a-z0-9 -]*(?:--[a-z][a-z0-9-]*(?:[= ][^ ]+)?)*)"
  }
}
