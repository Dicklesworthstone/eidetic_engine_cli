{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "focus_suggest_unimplemented",
  "honesty_only": true,
  "introduced_by": {
    "bead": "bd-sg5si",
    "epic_letter": "G"
  },
  "surfaces": [
    "focus suggest"
  ],
  "severity": "info",
  "repair_present": true,
  "trigger": {
    "description": "`ee focus suggest` is in Phase 1: the CLI surface and `ee.focus.suggest.v1` data schema are pinned, but the recent-CASS scoring and graph-centrality ranking are not yet wired. The command emits an empty `recommendations` array and this informational degraded entry until the follow-up `implements-surface:focus_suggest` bead lands the actual computation.",
    "setup_commands": [],
    "invocation": "ee focus suggest --json"
  },
  "expected_emission": {
    "code": "focus_suggest_unimplemented",
    "severity": "info",
    "message_contains": [
      "ee focus suggest is in Phase 1",
      "implements-surface:focus_suggest"
    ],
    "repair_contains": "ee context",
    "repair_string": "Use `ee context` or `ee search` until focus-suggest centrality lands."
  }
}
