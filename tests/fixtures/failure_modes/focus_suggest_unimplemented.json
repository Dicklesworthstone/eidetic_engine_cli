{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "focus_suggest_unimplemented",
  "honesty_only": true,
  "retired": true,
  "retired_by": {
    "bead": "bd-1idcb",
    "reason": "Phase 2 ranking pipeline landed: the `ee focus suggest` surface now populates `recommendations[]` from a real CASS-span + graph-centrality scoring path, so the honesty-only sentinel was removed from src/cli/mod.rs. The fixture remains as a tombstone per the SCHEMA.md retired-fixture pattern so tooling can still recognize the historical code, but the e2e driver asserts the production emission is absent."
  },
  "introduced_by": {
    "bead": "bd-sg5si",
    "epic_letter": "G"
  },
  "surfaces": [
    "focus suggest"
  ],
  "severity": "info",
  "repair_present": true,
  "trigger": {
    "description": "`ee focus suggest` was in Phase 1: the CLI surface and `ee.focus.suggest.v1` data schema were pinned, but the recent-CASS scoring and graph-centrality ranking were not yet wired. The command emitted an empty `recommendations` array and this informational degraded entry until the follow-up `implements-surface:focus_suggest` bead (bd-1idcb) landed the actual computation.",
    "setup_commands": [],
    "invocation": "ee focus suggest --json"
  },
  "expected_emission": {
    "code": "focus_suggest_unimplemented",
    "severity": "info",
    "message_contains": [
      "ee focus suggest is in Phase 1",
      "implements-surface:focus_suggest"
    ],
    "repair_contains": "ee context",
    "repair_string": "Use `ee context` or `ee search` until focus-suggest centrality lands."
  }
}
