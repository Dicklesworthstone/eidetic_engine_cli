{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "memory_health_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "status"
  ],
  "severity": "low",
  "repair_present": true,
  "trigger": {
    "description": "Memory health cannot be inspected without a selected and initialized workspace database.",
    "setup_commands": [],
    "invocation": "ee status --json"
  },
  "expected_emission": {
    "code": "memory_health_unavailable",
    "severity": "low",
    "message_contains": [
      "Memory health is unavailable",
      "workspace"
    ],
    "repair_contains": "ee status --workspace",
    "repair_string": "Run `ee status --workspace . --json`.",
    "repair_command_regex": "Run `(?<cmd>[^`]+)`"
  }
}
