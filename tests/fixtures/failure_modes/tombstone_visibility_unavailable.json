{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "tombstone_visibility_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "search"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "Search has hits from the index, but the memory database cannot be queried to filter tombstoned or expired records.",
    "setup_commands": [
      "ee init --workspace .",
      "# point search at an unreadable database while using a populated index fixture"
    ],
    "invocation": "ee search 'query' --workspace . --json"
  },
  "expected_emission": {
    "code": "tombstone_visibility_unavailable",
    "severity": "medium",
    "message_contains": [
      "could not verify tombstone visibility"
    ],
    "repair_contains": "ee doctor"
  }
}
