{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "feedback_protected_rules_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "status"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "Protected procedural rule rows cannot be read from the workspace database.",
    "setup_commands": [
      "ee init --workspace .",
      "ee diag database-skew --workspace . --output-database <workspace-with-protected-rule-skew>/.ee/ee.db --skew procedural-rules-table-unavailable --json"
    ],
    "invocation": "ee --fields full status --workspace <workspace-with-protected-rule-skew> --json"
  },
  "expected_emission": {
    "code": "feedback_protected_rules_unavailable",
    "severity": "medium",
    "message_contains": [
      "Protected procedural rule rows",
      "could not be read"
    ],
    "repair_contains": "ee db migrate",
    "repair_string": "Run `ee db migrate --workspace .`.",
    "repair_command_regex": "Run `(?<cmd>[^`]+)`"
  }
}
