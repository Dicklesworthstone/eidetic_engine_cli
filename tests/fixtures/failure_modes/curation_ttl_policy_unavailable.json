{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "curation_ttl_policy_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "status"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "Curation TTL policy rows cannot be read from the workspace database.",
    "setup_commands": [
      "ee init --workspace .",
      "ee diag database-skew --workspace . --output-database <workspace-with-ttl-policy-skew>/.ee/ee.db --skew curation-ttl-policies-table-unavailable --json"
    ],
    "invocation": "ee --fields full status --workspace <workspace-with-ttl-policy-skew> --json"
  },
  "expected_emission": {
    "code": "curation_ttl_policy_unavailable",
    "severity": "medium",
    "message_contains": [
      "Curation TTL policy rows",
      "could not be read"
    ],
    "repair_contains": "ee db migrate",
    "repair_string": "Run `ee db migrate --workspace .`.",
    "repair_command_regex": "Run `(?<cmd>[^`]+)`"
  }
}
