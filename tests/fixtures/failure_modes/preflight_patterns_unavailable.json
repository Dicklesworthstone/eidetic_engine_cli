{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "preflight_patterns_unavailable",
  "introduced_by": {
    "bead": "bd-3usjw.6",
    "epic_letter": "CLOSE_THE_GAP"
  },
  "surfaces": [
    "preflight check"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "The destructive-command pattern catalog cannot be loaded, so the preflight trauma guard cannot classify the command safely.",
    "setup_commands": [
      "ee init --workspace .",
      "move or corrupt tests/fixtures/destructive_patterns in an isolated fixture workspace"
    ],
    "invocation": "ee preflight check --cmd 'rm -rf build-output' --workspace . --json"
  },
  "expected_emission": {
    "code": "preflight_patterns_unavailable",
    "severity": "medium",
    "message_contains": [
      "destructive pattern",
      "unavailable"
    ],
    "repair_contains": "Restore the destructive pattern catalog",
    "repair_string": "Restore the destructive pattern catalog, then rerun ee preflight check --cmd <command> --json.",
    "repair_safety": [
      {
        "appliesTo": "Restore the destructive pattern catalog, then rerun ee preflight check --cmd <command> --json.",
        "command": null,
        "riskClass": "unavailable_or_manual_only",
        "preflightCommand": null,
        "requiresHumanApproval": true,
        "mutatesExternalState": false,
        "mutatesTrackerState": false,
        "privacyClass": "no_command",
        "nextAction": "manual_only",
        "ruleId": "repair_safety:unavailable_or_manual_only",
        "source": "repair_action_safety",
        "reasonCode": "manual_only_repair",
        "evidence": [
          "recovery_kind_none"
        ],
        "preconditions": [
          "operator_decision_required"
        ],
        "manualStep": "Restore the pattern catalog before classifying the exact command; do not run placeholder repair text."
      }
    ]
  }
}
