{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "integrity_provenance_sample_unavailable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "diag integrity"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "Integrity diagnostics cannot inspect the requested provenance-chain sample.",
    "setup_commands": [
      "ee init --workspace .",
      "ee diag database-skew --workspace . --output-database ./integrity-provenance-skew.db --skew memories-provenance-chain-hash-column-unavailable --json"
    ],
    "invocation": "ee diag integrity --database ./integrity-provenance-skew.db --workspace . --json"
  },
  "expected_emission": {
    "code": "integrity_provenance_sample_unavailable",
    "severity": "medium",
    "message_contains": [
      "provenance-chain sample",
      "could not be inspected"
    ],
    "repair_contains": "ee diag integrity",
    "repair_string": "ee diag integrity --json",
    "repair_command_regex": "(?<cmd>ee [a-z][a-z0-9 -]*(?:--[a-z][a-z0-9-]*(?:[= ][^ ]+)?)*)"
  }
}
