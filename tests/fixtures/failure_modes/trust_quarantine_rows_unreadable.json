{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "trust_quarantine_rows_unreadable",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "quarantine",
    "status"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "The quarantine report opens the ee database but cannot read source trust quarantine rows.",
    "setup_commands": [
      "ee init --workspace .",
      "# use a harness fixture with unreadable or migrated-away trust_quarantine storage"
    ],
    "invocation": "ee diag quarantine list --workspace . --json"
  },
  "expected_emission": {
    "code": "trust_quarantine_rows_unreadable",
    "severity": "medium",
    "message_contains": [
      "Failed to read source trust quarantine rows"
    ],
    "repair_contains": "ee db migrate",
    "repair_string": "ee db migrate --workspace .",
    "repair_command_regex": "(?<cmd>ee [a-z][a-z0-9 -]*(?:--[a-z][a-z0-9-]*(?:[= ][^ ]+)?)*)"
  }
}
