{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "snapshot_pin_expired",
  "introduced_by": {
    "bead": "bd-2caru.6",
    "epic_letter": "SRR"
  },
  "surfaces": [
    "context",
    "status"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "A read-side SnapshotPin remains active longer than storage.read_pool.max_pin_duration_seconds, so the lifecycle watchdog poisons the pin and prevents stale reads from continuing silently.",
    "setup_commands": [
      "ee config set storage.read_pool.max_pin_duration_seconds 1",
      "start a context/read-pool workflow that holds its read snapshot beyond the max pin duration"
    ],
    "invocation": "ee context 'read with expired snapshot pin' --workspace . --json"
  },
  "expected_emission": {
    "code": "snapshot_pin_expired",
    "severity": "medium",
    "message_contains": [
      "snapshot pin",
      "expired"
    ],
    "repair_contains": "storage.read_pool.max_pin_duration_seconds"
  }
}
