{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "daemon_background_mode_unimplemented",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "daemon"
  ],
  "severity": "low",
  "repair_present": true,
  "trigger": {
    "description": "Daemon status reporting explains that only bounded foreground daemon mode exists today.",
    "setup_commands": [
      "none"
    ],
    "invocation": "ee daemon --json"
  },
  "expected_emission": {
    "code": "daemon_background_mode_unimplemented",
    "severity": "low",
    "message_contains": [
      "Only bounded foreground daemon mode"
    ],
    "repair_contains": "ee daemon --foreground"
  }
}
