{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "learn_decay_config_read_failed",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "maintenance run"
  ],
  "severity": "medium",
  "repair_present": true,
  "trigger": {
    "description": "A maintenance run with decay enabled cannot read the workspace decay config path.",
    "setup_commands": [
      "ee init --workspace .",
      "mkdir -p .ee/config.toml"
    ],
    "invocation": "ee maintenance run --include-decay --workspace . --json"
  },
  "expected_emission": {
    "code": "learn_decay_config_read_failed",
    "severity": "medium",
    "message_contains": [
      "decay config",
      "read"
    ],
    "repair_contains": "config.toml",
    "repair_string": "Check .ee/config.toml permissions and rerun ee maintenance run --include-decay --json."
  }
}
