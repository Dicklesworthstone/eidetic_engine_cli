{
  "schema": "ee.failure_mode_fixture.v1",
  "code": "maintenance_job_history_write_failed",
  "introduced_by": {
    "bead": "bd-17c65.10.6",
    "epic_letter": "J"
  },
  "surfaces": [
    "job run"
  ],
  "severity": "high",
  "repair_present": true,
  "trigger": {
    "description": "A maintenance job runs, but its history row cannot be appended to the workspace .ee directory.",
    "setup_commands": [
      "ee init --workspace .",
      "# make the workspace .ee directory unwritable through a test harness"
    ],
    "invocation": "ee job run decay_sweep --workspace . --json"
  },
  "expected_emission": {
    "code": "maintenance_job_history_write_failed",
    "severity": "high",
    "message_contains": [
      "history"
    ],
    "repair_contains": "Check workspace .ee directory permissions"
  }
}
